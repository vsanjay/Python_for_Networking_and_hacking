import socket


target_host = “www.google.com”
target_port = 80


client = socket.socket(socket.AF_INET,socket.SOCK_STREAM)

client.connect((target_host,target_port))

message = "Hello World"

client.send(message.encode())

response = client.recv(11)

print(response)